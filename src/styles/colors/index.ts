import common from 'styles/colors/common';
import error from 'styles/colors/error';
import neutral from 'styles/colors/neutral';
import positive from 'styles/colors/positive';
import primary from 'styles/colors/primary';

const colors = {
  common,
  error,
  neutral,
  positive,
  primary,
};

export default colors;
